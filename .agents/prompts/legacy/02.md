Você é um engenheiro senior de software com vasta experiencia em linux, bash, programacao para shell e integracao de shell com ferramentas, tools, secrets e workflor, inlcuindo github actions e todo tipo de devops avancada, seja em ambiente de servidores, containers, virtualizadores ou em maquina de desenvolvimento local.

Hoje temos um problema, mesmo executando o boostrap do linux wsl nesse repositório atual do dotfiles, e o processo sendo executado sem erros, algumas ferramentas que deveriasm estar logadas e autenticadas, automaticamente não estão, como o "op" que é o cli do 1password, por vezes o "gh" que é o cli do github assim como outras.

Você já leu o repo atual do dotfiles e entendeu que ele, faça uma releitura, completa, cobrindo todos os arquivos do repositório, menos os caches de venv e outros caches de gerenciadores de ambientes, como o poetry e o pip. Fora isso, leia completamente o repositório, entenda suas lógicas, especificidades e funcionalidades, com base nisso atualize as documentacoes que gerou anteriormente, para que possamos resolver o problema. Os materiais anteriores foram movidos para `.agents/prompts/legacy/01`.

Uma vez feito isso, estará munido de contexto suficiente para o próximo passo, entender o problema, sua origem, o que acontece por debaixo dos panos e como resolver. Então, para finalizar, faça todos os testes possiveis para garantir que o repositorio funcione como foi concebido para funcionar, deixando tudo autenticado, logado e um ambiente 100% pronto para iniciar uma nova workstation de desenvolvimento com as principais ferramentas de gerenciamento de repositório, ssh agents, 1password, github gli, sops, age, secrets locais, remotos e em variáveis. env vars protegidas ou não, commits assinados e multi ambientes (windows e linux wsl) funcionais como deveriam estar.

Pode rodar o boostrao linux wsl para testar e verificar se o problema persiste se for uma das etapas necessárias. Executer quaisquer outras etapas necessárias para garantir que o boostrap linux wsl funcione como foi concebido para funcionar, deixando tudo autenticado, logado e um ambiente 100% pronto para iniciar uma nova workstation de desenvolvimento com as principais ferramentas de gerenciamento de repositório, ssh agents, 1password, github gli, sops, age, secrets locais, remotos e em variáveis. env vars protegidas ou não, commits assinados e multi ambientes (windows e linux wsl) funcionais como deveriam estar.
