## Regra global

- regionalizacao canonica do projeto e o default para qualquer semantica de
  data e hora
- `UTC` so existe por excecao explicita e rastreavel
- toda superficie temporal deve cair em `default_regionalized`,
  `explicit_utc` ou `stable_machine_token`
