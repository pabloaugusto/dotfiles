#!/usr/bin/env bash
set -euo pipefail

MSG_FILE="${1:?commit message file is required}"
ROOT_DIR="$(git rev-parse --show-toplevel 2>/dev/null || pwd)"
bash "${ROOT_DIR}/scripts/run-python.sh" "${ROOT_DIR}/.githooks/conventional_emoji.py" --validate --require-emoji "${MSG_FILE}"
