extends: default

ignore: |
  .cache/
  .venv/
  archive/
  uv.lock

rules:
  comments: disable
  document-start: disable
  line-length: disable
  truthy:
    allowed-values:
      - "true"
      - "false"
      - "on"
      - "off"
    check-keys: false
